-- ============================================================
-- V14: 소셜 로그인 유저 온보딩 완료 여부(is_onboarded) 컬럼 추가
-- ============================================================
--
-- 기존 가입 유저는 이미 온보딩을 마친 회원이므로 DEFAULT b'1'(true)로 설정합니다.
-- 신규 소셜 가입 유저는 false(b'0')로 시작하여 온보딩(프로필 수정) 완료 시 true(b'1')로 전이됩니다.

ALTER TABLE `users` ADD COLUMN `is_onboarded` bit(1) NOT NULL DEFAULT b'1';
