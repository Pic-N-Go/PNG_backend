# 2026 여름 역량 강화 로드맵
### 백엔드 개발자 취업 경쟁력 극대화 (26 하반기 / 27 상반기 대비)

> **핵심 전략**: "누구나 하는 CRUD 포트폴리오"에서 벗어나, **"LLM을 프로덕션 수준으로 안정적으로 서빙하는 백엔드 개발자"**로 포지셔닝한다. 이미 가진 무기(캐싱·비동기·서킷브레이커·Elasticsearch)를 LLM이라는 새 대상에 적용하는 것이 핵심. 새로 다 배우는 게 아니라 **기존 강점의 확장**이다.

---

## 📌 이 문서의 사용법

- 각 항목 앞의 `[ ]`를 `[x]`로 바꿔가며 진행 상황을 체크한다.
- **우선순위 표기**: ⭐⭐⭐ = 반드시 / ⭐⭐ = 강력 권장 / ⭐ = 여유되면
- Pic N Go 하나로 Part 1의 대부분을 커버할 수 있도록 설계했다.
- 면접에서 파고들 거리를 만드는 게 목표다. "붙였다"가 아니라 "이 문제를 이렇게 풀었다"로 말할 수 있어야 한다.

---

# Part 1. LLM 활용 역량 (이번 여름 최우선 차별점)

## 개념 정리 — 내가 무엇을 하는 사람인가

- **LLM** = AI로 텍스트를 생성하는 프로그램. 개발자 입장에선 **"까다로운 외부 API 하나"**일 뿐이다.
- **LLM 서빙** = 이 까다로운 API를 실제 서비스에 **안정적으로 붙이는 것**. 백엔드 개발자가 실력을 증명하는 지점.
- 기업이 원하는 AI 역량 = **"AI API를 (1) 안정적으로 서빙하면서 (2) 원하는 품질의 결과를 뽑아내도록 관리하는 역량"**

### LLM API가 일반 API와 다른 점 (이게 왜 "역량"이 되는가)

| 일반 API (CODEF 등) | LLM API | 그래서 필요한 대응 |
|---|---|---|
| 응답 빠름 (수십~수백 ms) | **느림 (수 초)** | 스트리밍, 캐싱 |
| 거의 공짜 | **호출당 과금 (토큰)** | 응답 캐싱, 프롬프트 최적화 |
| 같은 입력 → 같은 출력 | **매번 다른 출력** | Structured Output, 검증 |
| 형식 일정 | **형식이 깨질 수 있음** | JSON 스키마 강제 |
| 웬만하면 성공 | **rate limit·timeout 잦음** | 서킷브레이커, 폴백, 재시도 |

---

## 1-A. LLM 서빙 인프라 ⭐⭐⭐ (주력 — 코드로 증명되는 실력)

> 이게 CRUD 포트폴리오와 갈리는 핵심 지점이다. 기존 백엔드 무기가 그대로 먹히는 영역이라 가성비 최고.

### [ ] 1. 기본 API 호출 (출발점)
- OpenAI 또는 Claude API를 Java/Spring에서 호출 (`RestClient` / `WebClient`)
- 요청·응답 구조 이해 (messages, model, token)
- ⚠️ **여기서 멈추면 "챗봇 붙였어요" 수준 = 감점.** 반드시 아래로 진행할 것

### [ ] 2. 비용·지연 제어
- **응답 캐싱**: 동일·유사 질의는 재호출 없이 캐시 반환 (Redis 활용 — 이미 다뤄본 것)
- **프롬프트 캐싱**: 반복되는 시스템 프롬프트 부분 캐싱으로 토큰 절감
- **스트리밍(SSE)**: 응답을 토큰 단위로 흘려보내 체감 지연 대폭 감소
- 📊 **정량 지표 확보**: "캐싱 적용 후 평균 응답 시간 X초→Y초, 월 예상 비용 Z% 절감" 식으로 뽑을 것

### [ ] 3. 실패 대응 (기존 인프라 재활용 — 스토리 매끄러움)
- **서킷브레이커** (Resilience4j — 이미 Pic N Go에 있음): LLM API 장애 시 회로 차단
- **폴백(Fallback)**: LLM이 죽으면 규칙 기반 결과라도 반환 (예: 기존 규칙 기반 여행 코스)
- **재시도(Retry)**: timeout·일시적 5xx에 지수 백오프 재시도
- **타임아웃 설정**: LLM은 느리므로 적절한 타임아웃 필수

### [ ] 4. 비결정성·형식 제어 (품질의 시작)
- **Structured Output**: JSON 스키마를 강제해 항상 정해진 형식으로 응답받기
- **응답 검증 + 재요청**: 파싱 실패 시 자동 재시도 로직
- 🎯 이걸 하면 "AI가 형식 안 깨지고 바로 DB/UI에 연결되도록 통제했다"는 스토리가 생김

### [ ] 5. 토큰·컨텍스트 관리
- 입력이 컨텍스트 윈도우를 넘을 때 잘라내거나 요약하는 전략
- 토큰 사용량 계산·추적

---

## 1-B. RAG + 벡터 검색 ⭐⭐⭐ (강한 차별점 — Elasticsearch 경험 직결)

> CozyStay에서 Elasticsearch를 이미 다뤄봤으니 여기로 확장하면 스토리가 아주 매끄럽다. **가성비 최고의 차별화 포인트.**

### [ ] 1. RAG 개념 이해
- **RAG(Retrieval-Augmented Generation)** = 우리 데이터를 검색해서 LLM에 주입 → LLM이 그 데이터 기반으로만 답하게 하는 것
- **왜 중요한가**: LLM 환각(없는 사실을 지어냄) 방지 + 최신·도메인 데이터 반영

### [ ] 2. 벡터 검색 구현 (택 1)
- **옵션 A — Elasticsearch 벡터 검색**: 이미 경험 있음 → 학습 비용 최소, 스토리 연결 최강 ✅ 추천
- **옵션 B — pgvector**: PostgreSQL 확장. 이미 RDB 쓰면 인프라 단순
- 임베딩(텍스트를 벡터로 변환) → 벡터 DB 저장 → 유사도 검색 흐름 이해

### [ ] 3. RAG 파이프라인 완성
- 사용자 질의 → 관련 데이터 검색 → LLM에 컨텍스트로 주입 → 답변 생성
- 🎯 **환각 제어 스토리 확보**: "LLM이 존재하지 않는 장소를 지어내지 않도록 실제 장소 데이터를 RAG로 주입했다"

---

## 1-C. Tool Calling / 에이전트 ⭐⭐ (요즘 가장 뜨거운 영역)

### [ ] 1. Tool Calling (함수 호출) 이해
- LLM이 외부 함수(DB 조회, API 호출)를 스스로 호출하게 만드는 것
- "LLM이 판단 → 우리 백엔드 함수 실행 → 결과를 다시 LLM에게" 흐름

### [ ] 2. MCP(Model Context Protocol) 개념 파악
- LLM에 도구·데이터를 표준 방식으로 연결하는 프로토콜 (최신 트렌드)
- 깊이 구현까진 아니어도 **개념 이해 + 면접 언급 가능 수준**은 확보

---

## 1-D. 출력 품질 관리 & 팀 협업 관점 ⭐⭐ (성숙도 어필)

> 개인 도구를 넘어 "AI를 팀 협업·운영 관점에서 이해한다"는 인상. 신입에게 필수는 아니지만 **이해의 폭**을 보여줌.

### [ ] 1. 프롬프트 엔지니어링 기본
- 명확한 지시, 예시 제공(few-shot), 역할 부여, 단계적 사고 유도
- 프롬프트 버전 관리 개념

### [ ] 2. 컨텍스트 엔지니어링 (지침 표준화)
- `CLAUDE.md` 같은 공통 지침 파일로 팀 전체가 일관된 AI 품질을 내게 하는 것
- 개인 레벨("내가 프롬프트를 잘 씀")을 넘어 팀 레벨("팀 규칙을 AI에 표준화")로 확장
- 💡 스터디 발표 주제로 활용하거나, 면접에서 "AI를 팀에 도입한다면 지침 표준화부터 하겠다"는 식으로 언급하면 인상적

### [ ] 3. 평가(Evaluation) — 여유되면 ⭐
- LLM 출력 품질을 어떻게 검증했는지 (테스트 셋, 자동 평가 로직)
- 이게 하나라도 있으면 성숙도가 확 올라감

### [ ] 4. 관측 가능성(Observability) — 여유되면 ⭐
- LLM 호출 로깅, 토큰 사용량·비용 추적, 프롬프트 버전 관리
- "운영을 생각하는 개발자"라는 인상

---

## 🎯 Pic N Go 통합 설계안 — Part 1을 하나로 묶기

여행 코스 생성 서비스에 아래처럼 붙이면 **1-A, 1-B를 한 프로젝트로 전부 증명**할 수 있다:

1. 사용자 자연어 입력("아이랑 갈 조용한 2박3일 코스") → **LLM이 코스 초안 생성**
2. **Structured Output**으로 코스를 정해진 JSON 스키마(장소/시간/이동)로 강제 → 파싱 실패 없이 DB·UI 연결
3. 실제 장소 데이터는 **RAG(Elasticsearch 벡터 검색)**로 주입 → LLM 환각으로 없는 장소 만드는 것 방지
4. 외부 LLM API 실패 시 **서킷브레이커 + 폴백**(기존 규칙 기반 코스 반환) → 기존 인프라 재활용
5. 생성 결과 **캐싱(Redis)**으로 비용·지연 절감 → 정량 지표 확보

> **서술 원칙**: "LLM을 붙였다"가 아니라 **"LLM을 프로덕션에서 안정적으로 서빙하기 위해 캐싱·구조화 출력·서킷브레이커·RAG를 적용했다"**로 쓴다. 면접관이 듣고 싶은 건 붙였다는 사실이 아니라, 붙이면서 생긴 문제를 어떻게 풀었느냐다.

---

# Part 2. 백엔드 기본기 심화 (LLM 역량을 받쳐주는 토대)

> AI 역량만으로는 안 된다. 결국 "탄탄한 백엔드 개발자"라는 토대 위에 AI가 얹혀야 설득력이 생긴다.

## 2-A. Spring 내부 동작 이해 ⭐⭐⭐

### [ ] Spring AOP 메커니즘
- 프록시 기반 동작 원리
- **자기 호출(self-invocation) 프록시 우회 문제** — 이미 학습한 내용, 면접 단골 질문

### [ ] 트랜잭션 심화
- `@Transactional` 전파 속성, 격리 수준
- 프록시와 트랜잭션의 관계 (AOP 이해와 연결)

### [ ] 의존성 주입·빈 생명주기
- 빈 스코프, 생성 시점, 순환 참조 문제

## 2-B. 데이터·동시성 ⭐⭐⭐

### [ ] 분산 락 (Redis)
- 분산 환경에서의 동시성 제어 — 이미 학습한 내용
- 락 획득·해제, 타임아웃, 안전성 이슈

### [ ] DB 최적화
- 커넥션 풀 튜닝 (MoneyJ 경험 — 정량 지표로 정리)
- 인덱스 설계, 쿼리 최적화
- N+1 문제와 해결 (JPA)

### [ ] 캐싱 전략
- 로컬 캐시 vs 분산 캐시, 캐시 무효화 전략
- LLM 응답 캐싱과 자연스럽게 연결됨

## 2-C. 회복탄력성 & 아키텍처 ⭐⭐

### [ ] Resilience4j 심화
- 서킷브레이커, 재시도, 벌크헤드, 레이트 리미터
- LLM 서빙의 실패 대응과 직결

### [ ] 비동기 처리
- `@Async`, `CompletableFuture`
- SSE 스트리밍 (LLM 응답 스트리밍의 기반)

### [ ] API 설계
- RESTful 설계 원칙, 에러 응답 표준화, 버전 관리

## 2-D. 인프라 & 배포 ⭐⭐

### [ ] Docker / Docker Compose
- 단일 EC2에 Docker Compose로 여러 서비스 운영 (비용 절감 데모 전략)

### [ ] 클라우드 기본
- AWS EC2, 또는 Oracle Cloud 무료 티어 활용
- CI/CD 기초 (GitHub Actions 등)

---

# Part 3. 취업 준비 실무 (여름 내내 병행)

## 3-A. 코딩 테스트 유지 ⭐⭐⭐
- [ ] 주 X회 꾸준히 문제 풀이 (감 유지가 목적)
- [ ] 자주 나오는 유형(DFS/BFS, DP, 그리디, 구현) 복습

## 3-B. 포트폴리오 정리 ⭐⭐⭐
- [ ] **MoneyJ** (fintech): 커넥션 풀 최적화·CODEF·RSA 암호화 — 정량 지표 명확히
- [ ] **CozyStay** (플랫폼/검색): Elasticsearch·OAuth
  - ⚠️ 250ms→30ms는 **두 개의 별개 개선**으로 서술 (엔진 교체=속도 / 가중치 로직 제거=정확도)
- [ ] **FinMate** (스타트업/레거시): Spring Legacy + MyBatis 리팩터링
- [ ] **Pic N Go** (여행 코스 + LLM): 이번 여름 주력 — Part 1 통합 설계안 반영
- [ ] ⚠️ 성능 지표에서 "가정하여" 같은 헤지 표현 제거 (신뢰성 약점으로 지적된 부분)

## 3-C. 서류(자소서·이력서) 고도화 ⭐⭐⭐
- [ ] 기업 유형별 맞춤화 (핀테크 / 플랫폼 / 스타트업 — 프로젝트 매칭)
- [ ] 두괄식, 숫자·행동 담긴 소제목, 선언형 마무리 지양
- [ ] 진짜 주니어 개발자 톤 (AI스럽지 않게)
- [ ] 16개 경험 스토리 뱅크 재활용

## 3-D. 스터디 & 지속 학습 ⭐⭐
- [ ] 주간 개발 스터디에서 이번 여름 학습 내용 발표 (LLM 서빙, RAG 등 — 아웃풋으로 정리)
- [ ] 기술 블로그 작성 (학습을 자산화, 면접 근거 확보)

---

# 🗓️ 여름 실행 순서 제안

**7월 (기반 다지기)**
1. 서류·이력서 전면 개편 (기존 계획 유지)
2. Pic N Go에 LLM 기본 API 호출 → Structured Output → 캐싱까지 (1-A 1~4)
3. 코딩 테스트 병행

**8월 초·중 (차별점 완성)**
4. Pic N Go에 서킷브레이커·폴백 통합 (1-A 3)
5. RAG(Elasticsearch 벡터 검색) 붙이기 (1-B) — **최대 차별점**
6. 정량 지표 측정·정리 (응답 시간, 비용 절감)

**8월 말 (마무리·자산화)**
7. Tool Calling / MCP 개념 학습 + 면접 답변 준비 (1-C)
8. 학습 내용 기술 블로그·스터디 발표로 정리 (3-D)
9. Pic N Go 성과를 포트폴리오·자소서에 통합 (3-B, 3-C)

---

# ✅ 핵심 요약 (한 장 정리)

| 영역 | 최우선 | 왜 | 근거(내 강점) |
|---|---|---|---|
| **LLM 서빙 인프라** | ⭐⭐⭐ | CRUD와 갈리는 핵심. 코드로 증명 | 캐싱·비동기·서킷브레이커 보유 |
| **RAG + 벡터검색** | ⭐⭐⭐ | 가성비 최고 차별점 | Elasticsearch 경험 |
| **비동기/스트리밍** | ⭐⭐ | LLM 응답 스트리밍 기반 | — |
| **Tool Calling/MCP** | ⭐⭐ | 최신 트렌드, 면접 어필 | — |
| **Spring 내부·동시성** | ⭐⭐⭐ | 백엔드 토대 | AOP·분산락 학습 완료 |
| **서류·포트폴리오** | ⭐⭐⭐ | 상반기 병목 지점 | 프로젝트 3+1개 보유 |

> **한 문장 결론**: 이번 여름의 목표는 **"LLM을 프로덕션 수준으로 안정적으로 서빙할 줄 아는, 기본기 탄탄한 백엔드 주니어"**로 자신을 증명하는 것. 남들이 "AI 붙였어요"에서 끝날 때, 나는 **"까다로운 LLM API를 캐싱·구조화 출력·서킷브레이커·RAG로 프로덕션 수준까지 안정화했어요"**까지 간다. 그리고 그 무기는 이미 내 손에 있는 것들의 확장이다.
