# Pic N Go (PNG) 고도화 전략

> 여행 코스 플래닝 서비스의 백엔드 고도화 아이디어 모음. 포트폴리오 아키텍처 다이어그램과 이력서 트러블슈팅 항목의 무기로 활용한다.

---

## 🔧 우선 해결할 기술 이슈 (선행 과제)

- [ ] 벌크 연산 에러
- [ ] 트랜잭션 범위
- [ ] 테스트 코드
- [ ] JPA List 중복 Insert 에러

---

# A. 기능 도메인별 고도화 아이디어

## 🎯 1. 알림 스케줄러 고도화 (대용량 / 비동기 처리)

**[배경]** 매일 아침 유저들에게 날씨(기상청) 예보 푸시 알림을 보내는 스케줄러 기능

### ① 대규모 데이터 배치 처리 (Spring Batch)
- **이슈**: 유저가 100만 명일 때 `@Scheduled` 안에서 for문으로 DB를 조회하면 메모리 초과(OOM) 발생
- **해결**: Spring Batch(Chunk)를 도입하여 1,000건씩 끊어서 안전하게 처리

### ② 비동기 이벤트 드리븐 (Kafka / RabbitMQ)
- **이슈**: 구글 FCM 서버에 알림을 동기적으로 보내면 외부 서버 지연 시 우리 서버도 같이 뻗음
- **해결**: 알림 발송 로직을 카프카(메시지 큐)에 던지고 즉시 리턴. 워커 서버들이 비동기로 꺼내어 발송하여 지연 해소

---

## 🚦 2. 외부 API 의존성 및 장애 격리

**[배경]** 카카오 길찾기 API(코스 동선)와 기상청 API(날씨) 연동 로직

### ③ API 타임아웃 장애 격리 (Circuit Breaker)
- **이슈**: 카카오/기상청 서버가 다운되어 응답이 없으면 우리 톰캣 스레드 풀이 꽉 차서 서비스 전체가 다운됨
- **해결**: Resilience4j 서킷 브레이커를 도입해 타임아웃 누적 시 회로를 차단(Fail-Fast)하고, 즉시 기본값(예: 30분)을 반환하는 Fallback 로직 적용

---

## ⚡ 3. 코스(Course) 도메인 동시성 및 캐싱 (Redis 활용)

**[배경]** 스팟을 추가하고 순서를 변경하는 코스 오케스트레이션 로직

### ④ 외부 API 비용 및 속도 최적화 (Redis Caching)
- **이슈**: 스팟 간 이동 시간을 구할 때마다 카카오 API를 호출하면 과금(Rate Limit) 및 속도 저하 발생
- **해결**: (출발지ID, 도착지ID)를 Key로 하여 소요 시간을 Redis에 7일간 캐싱(Look-Aside). API 호출 비용과 응답 시간을 90% 이상 절약

### ⑤ 공동 편집 데이터 정합성 보장 (Redis 분산락)
- **이슈**: 다수의 유저가 동시에 한 코스의 스팟 순서를 변경(`updateSpotOrder`)할 때 발생하는 데드락 및 데이터 덮어쓰기(Lost Update) 문제
- **해결**: DB 락의 부하를 피하기 위해 Redis Redisson 분산락을 도입해 안전하게 동시성 제어

---

## 🔔 4. 알림 중복 및 부하 방어 (Redis 활용)

**[배경]** 매일 발송되는 FCM 알림 로직

### ⑥ 분산 환경 알림 중복 발송 방지 (Redis SETNX)
- **이슈**: 다중 서버 환경이거나 배포 실수로 스케줄러가 2번 실행되면 유저에게 동일한 푸시 알림이 2번 발송되는 대참사 발생
- **해결**: 발송 직전 Redis에 `push_sent:유저ID:날짜` Key를 생성(SETNX). 이미 Key가 존재하면 중복 발송으로 간주하고 스킵하여 멱등성(Idempotency) 보장

### ⑦ DND(방해금지) 필터링 캐싱
- **이슈**: 10만 명에게 알림을 보낼 때마다 DB를 조회해 방해금지 여부를 체크하면 커넥션 낭비
- **해결**: 방해금지 유저 목록을 Redis에 글로벌 캐싱하여 초고속으로 필터링 처리

---

### 💡 최종 이력서(포트폴리오) 추천 콤보 — Best 3

당장 이력서에 썼을 때 "이 지원자는 진짜 현업의 문제를 깊게 고민했구나" 하고 면접관의 고개를 끄덕이게 만들 조합:

1. **[비용 절감]** 카카오 API 이동 시간 Redis 인메모리 캐싱 (④)
2. **[안정성]** 알림 중복 발송 완벽 방어 Redis SETNX (⑥)
3. **[확장성]** 100만 유저 타겟 푸시 발송 최적화 Kafka 비동기 처리 (①·② 섞기)

> 포트폴리오 아키텍처 다이어그램이나 이력서 트러블슈팅 항목에 한 줄씩 추가하면 완벽한 무기가 된다.

---

# B. 우선순위별 고도화 테크트리 (검색·조회 중심)

## 🥇 1순위: 대용량 장소(Spot) 검색 최적화 (PostgreSQL 전문검색 & pgvector)
**관련 번호: 24, 7, 20**

> **기술 선택 근거 (면접 대비 필수 스토리)**: 초기엔 Elasticsearch를 검토했으나, 개인 프로젝트/소규모 서비스 단계에서 별도 검색 서버(JVM 기반, 최소 2GB 힙)를 띄우는 것은 **인프라 비용·운영 부담이 과도**하다고 판단. PostgreSQL 하나로 전문 검색과 벡터 검색을 모두 처리하여 **단일 EC2 + Docker Compose로 운영 단순화**. "규모가 커지면 전용 검색 엔진으로 이관"하는 확장 경로까지 설계에 반영. → **비용을 고려한 기술 선택 판단력** 어필.

- **도입 명분**: 여행 앱의 심장은 '검색'. 수백만 개의 전국 장소 데이터에서 "강남역 감성 카페"를 검색할 때 `LIKE '%강남역%'`를 쓰면 인덱스를 타지 못해 풀스캔이 발생하고 DB가 뻗어버림
- **고도화 스토리**:
  - 기존 LIKE 쿼리의 성능적 한계 및 인덱스 풀스캔 병목을 JMeter로 증명
  - **PostgreSQL Full-Text Search** 도입: `tsvector` / `tsquery`와 **GIN 인덱스**로 키워드 검색을 인덱스 기반으로 전환. (한국어 형태소가 더 필요하면 `pg_bigm`(bigram) 확장으로 부분 일치 정확도 보완)
  - **pgvector 확장**으로 의미 기반(semantic) 검색까지 확보: 장소 설명을 임베딩해 벡터로 저장하고, "감성 카페" 같은 뉘앙스 질의를 벡터 유사도로 검색. **HNSW 인덱스**로 근사 최근접 탐색(ANN) 속도 최적화
  - 별도 검색 서버가 없으므로 **데이터 동기화 파이프라인(CDC/Logstash) 자체가 불필요** — 원본 테이블과 검색 인덱스가 같은 DB에 있어 정합성 이슈 원천 제거
- **면접관 어필 포인트**: 검색 엔진을 남발하지 않고 **RDB의 기능(GIN 인덱스, 전문검색, pgvector)을 깊이 활용**해 문제를 해결하는 실무 감각. "왜 ES를 안 썼는가"에 명확한 비용·운영 근거로 답할 수 있음

## 🥈 2순위: 메인 피드 대규모 조회 최적화 (No-Offset 페이징 & 인메모리 캐시)
**관련 번호: 8, 2, 1, 19**

- **도입 명분**: 유저가 홈 화면에서 '인기 스팟'·'공개 여행 코스' 피드를 무한 스크롤로 구경. 뒤로 갈수록 느려지는 Offset 페이징과 조회 시마다 발생하는 N+1 쿼리 폭탄은 치명적 약점
- **고도화 스토리**:
  - `LIMIT 1000000, 20` 방식의 쿼리 지연을 커서 기반(No-Offset) 페이징과 커버링 인덱스로 변경 → 항상 일정한(O(1)) 조회 속도 보장
  - 조회 시 터지는 Course-Spot-Checklist의 N+1 문제를 Fetch Join과 BatchSize로 1차 방어
  - 실시간성이 덜 중요한 '어제의 주간 인기 코스 TOP 10'은 Redis 인메모리 캐시 적용. 만료 시점 트래픽 몰림으로 DB가 터지는 캐시 스탬피드(Cache Stampede) 방어 로직 추가
- **면접관 어필 포인트**: 백엔드 기본기인 '조회 최적화'의 끝판왕(페이징 → 쿼리 튜닝 → 캐싱)을 모두 다뤄본 탄탄한 기본기 어필

## 🥉 3순위: 대규모 푸시 알림 & 미디어 처리 (Kafka 이벤트 드리븐)
**관련 번호: 10, 34, 28**

- **도입 명분**: 리뷰 작성 시 고화질 사진을 서버가 직접 받아 압축하고 푸시 알림까지 보내면 서버 CPU가 터짐 (기존 스케줄러 문제와 일맥상통)
- **고도화 스토리**:
  - 이미지는 서버를 거치지 않고 S3 Presigned URL을 통해 클라이언트가 S3로 직행 업로드 (서버 네트워크 부하 제로화)
  - 이미지 썸네일 리사이징과 알림 발송은 **Kafka(또는 RabbitMQ)** 로 메시지만 던지고 응답은 0.1초 만에 반환 (비동기 이벤트 드리븐)
- **면접관 어필 포인트**: MSA로 넘어가기 위한 필수 관문인 '메시지 큐를 활용한 결합도 낮추기'와 '비동기 아키텍처' 설계 능력 어필

## 🏅 4순위: 코스 공동 편집 정합성 보장 (Redis 분산락)
**관련 번호: 3, 9**

- **도입 명분**: 노션처럼 여러 친구가 동시에 한 여행 코스에 들어와 스팟을 추가·삭제할 때 발생하는 동시성 충돌(Race Condition)
- **고도화 스토리**:
  - 다중 서버 환경을 가정하고 기존 자바 락(`synchronized`)이 무용지물임을 설명
  - DB 락 대신 Redis Redisson 분산락 도입. Pub/Sub 방식으로 락 획득 대기열을 만들어 무분별한 재시도(Spin Lock) 부하를 줄이면서 안전하게 순서 정합성 보장
- **면접관 어필 포인트**: 트래픽이 몰리는 분산 환경에서 '데이터 정합성'을 지켜본 경험 어필 (이커머스의 쿠폰 발급/재고 감소 급으로 매력적인 주제)

## 🏅 5순위: 공공데이터 대량 배치(Batch) 적재 최적화
**관련 번호: 11**

- **도입 명분**: 초기 런칭 시 한국관광공사 API에서 10만 개의 장소 데이터를 긁어와 매일 새벽 3시에 DB에 Upsert 해야 함
- **고도화 스토리**:
  - JPA의 `saveAll()`로 인해 10만 번의 Insert 쿼리가 단건으로 날아가는 심각한 병목 발견
  - Spring Batch로 에러 발생 시 부분 재시작(Restart)을 지원하도록 설계
  - JDBC `executeBatch()`를 곁들여 벌크 인서트(Bulk Insert)로 쿼리 횟수를 기하급수적으로 단축
- **면접관 어필 포인트**: 실무 백엔드 개발자의 필수 소양인 '배치 프로세싱'과 '대용량 데이터 마이그레이션' 경험 어필

---

# C. LLM 기반 고도화 (⭐ 최대 차별점 — 남들과 갈리는 지점)

> A·B는 "정통 백엔드 고도화"라 강력하지만 잘하는 지원자가 많다. 진짜 변별력은 여기서 나온다. 핵심 서술 원칙은 **"LLM을 붙였다"가 아니라 "까다로운 LLM API를 프로덕션 수준으로 안정적으로 서빙했다"**. 이미 가진 무기(캐싱·서킷브레이커·PostgreSQL 검색)를 LLM이라는 새 대상에 그대로 적용하는 구조라 스토리가 매끄럽다.

## 🤖 8. LLM 기반 여행 코스 자동 생성 (프로덕션 서빙)

**[배경]** 사용자가 자연어("아이랑 갈 조용한 2박3일 코스")로 입력하면 LLM이 코스를 생성하는 기능. LLM API는 일반 API와 달리 **느리고(수 초), 호출당 과금되며, 출력이 매번 다르고 형식이 깨질 수 있고, rate limit·timeout이 잦다.** 이 까다로움을 어떻게 통제했는가가 핵심.

### ⑧ 출력 형식 강제 및 검증 (Structured Output)
- **이슈**: LLM 응답은 형식이 일정하지 않아 그대로 파싱하면 깨지고, 바로 DB·UI에 연결할 수 없음
- **해결**: JSON 스키마(장소/시간/이동)를 강제하는 Structured Output 적용. 파싱 실패 시 자동 재요청 로직으로 항상 정해진 형식만 반환하도록 통제
- **면접관 어필 포인트**: "AI를 붙였다"가 아니라 "AI 출력을 시스템이 신뢰할 수 있는 데이터로 통제했다"는 성숙도 어필

### ⑨ 환각(Hallucination) 방지 (RAG + pgvector 벡터 검색)
- **이슈**: LLM이 실제로 존재하지 않는 장소·주소를 그럴듯하게 지어내는 환각 문제. 여행 서비스에서 없는 장소를 추천하면 치명적
- **해결**: 실제 장소 데이터를 **PostgreSQL pgvector 벡터 검색(RAG)**으로 검색해 LLM에 컨텍스트로 주입 → LLM이 우리 DB의 실존 장소 기반으로만 코스를 구성하도록 제약. 장소 임베딩을 벡터 컬럼에 저장하고 HNSW 인덱스로 유사도 검색
- **면접관 어필 포인트**: 1순위 검색(pgvector)과 **완전히 동일한 인프라를 재활용** — 별도 벡터 DB 없이 기존 PostgreSQL 하나로 검색과 RAG를 모두 처리. 검색 엔진 + LLM 결합 파이프라인을 최소 비용으로 구현한 설계력 어필

### ⑩ LLM API 장애 격리 및 폴백 (Circuit Breaker)
- **이슈**: 외부 LLM API가 느려지거나(수 초~수십 초) 다운되면, 동기 호출 시 톰캣 스레드 풀이 꽉 차 서비스 전체가 마비됨. 일반 외부 API보다 지연·실패가 훨씬 잦음
- **해결**: Resilience4j 서킷 브레이커로 타임아웃 누적 시 회로 차단(Fail-Fast). LLM이 죽으면 **기존 규칙 기반 코스 생성 로직으로 폴백**하여 서비스 연속성 보장 (②③의 인프라 재활용)
- **면접관 어필 포인트**: LLM을 "언제든 실패할 수 있는 외부 의존성"으로 냉정하게 다룬 실전 감각 어필

### ⑪ LLM 비용·지연 최적화 (Redis 캐싱 + 스트리밍)
- **이슈**: LLM 호출은 토큰당 과금되어 비싸고, 응답이 수 초 걸려 사용자 체감 속도가 나쁨. 유사·동일 질의를 매번 새로 호출하면 비용·지연이 누적됨
- **해결**:
  - **응답 캐싱**: 유사 질의 결과를 Redis에 캐싱(Look-Aside)해 재호출 없이 반환 → 비용·지연 절감 (④의 캐싱 전략 재활용)
  - **스트리밍(SSE)**: 생성된 코스를 토큰 단위로 흘려보내 체감 대기 시간 대폭 감소
- **면접관 어필 포인트**: 📊 정량 지표 확보 지점 — "캐싱 적용 후 평균 응답 X초→Y초, 월 예상 API 비용 Z% 절감"

### ⑫ (여유되면 ⭐) Tool Calling / 출력 품질 평가
- **Tool Calling**: LLM이 직접 우리 백엔드 함수(날씨 조회, 이동 시간 계산)를 호출하게 하여 실시간 정보를 반영한 코스 생성
- **평가(Evaluation)**: LLM이 생성한 코스가 제약(예산·동선·시간)을 지켰는지 검증하는 자동 평가 로직 → 출력 품질 관리 성숙도 어필

> **⑧~⑪ 통합 흐름**: 자연어 입력 → RAG로 실제 장소 검색·주입(⑨) → LLM이 코스 생성 → JSON 스키마로 강제·검증(⑧) → LLM 실패 시 규칙 기반 폴백(⑩) → 결과 캐싱·스트리밍(⑪). 이 하나의 파이프라인으로 **"LLM 프로덕션 서빙"**을 통째로 증명한다.

---

## 💡 가장 추천하는 이력서 테크트리 조합

### 기본 조합 (정통 백엔드 3대장)
> **[ 1순위(PostgreSQL 검색 + pgvector) + 3순위(Kafka 비동기) + 4순위(Redis 분산락) ]**

이 3가지를 포트폴리오 메인 아키텍처 다이어그램에 그려 넣으면, **'DB 최적화 – 비동기 메시지 큐 – 동시성 제어'**라는 백엔드 채용 공고의 핵심 우대사항 3대장을 모두 충족하게 된다.

### 🔥 차별화 조합 (기본 3대장 + LLM 서빙)
> **[ 위 3대장 + C. LLM 코스 생성(⑧ Structured Output + ⑨ RAG + ⑩ 서킷브레이커 폴백 + ⑪ 캐싱) ]**

정통 백엔드 3대장만으로는 "잘한 지원자"에서 끝난다. 여기에 **LLM 프로덕션 서빙 파이프라인**을 얹으면 "AI를 안정적으로 서빙할 줄 아는, 기본기 탄탄한 백엔드 주니어"로 포지셔닝된다. 특히 ⑨(RAG)가 1순위(pgvector)와 **완전히 동일한 인프라를 공유**하고, ⑩(폴백)이 ③(서킷브레이커)과, ⑪(캐싱)이 ④(Redis 캐싱)와 맞물려 **하나의 일관된 아키텍처로 자연스럽게 통합**된다. 별도 검색 서버·벡터 DB 없이 PostgreSQL·Redis만으로 이 모든 걸 구현했다는 점 자체가 **비용 효율적 설계**로 어필된다. 이게 이 포트폴리오의 최종 무기다.

> **한 문장 서술 예시**: "외부 LLM API의 지연·환각·비결정성 문제를 RAG(pgvector)·Structured Output·서킷브레이커 폴백·Redis 캐싱으로 통제하여, AI 코스 생성 기능을 프로덕션 수준으로 안정화했습니다. 별도 검색 인프라 없이 PostgreSQL 하나로 전문검색과 벡터검색을 통합해 운영 비용을 최소화했습니다."
